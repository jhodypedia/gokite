NODE_ENV=production
PORT=3000

DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=
DB_NAME=kiteai
DB_DIALECT=mysql

SESSION_SECRET=supersecret_kiteai

# Worker
RUN_INTERVAL_MS=300000   # 5 menit

# OPTIONAL proxy (per baris; format host:port atau user:pass@host:port)
PROXY_LIST=

# (Wajib disesuaikan) – token generator untuk endpoint /v2/signin
GENERATOR_SECRET=replace-me
